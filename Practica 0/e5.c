/**
 * El tipo de la funcion malloc es void pointer. O sea, cuando allocamos
 * memoria a traves de malloc, el retorno es un puntero a la primera di-
 * reccion de memoria del espacio que allocamos. Si la funcion no fuera
 * capaz de reservar el espacio solicitado, retorna NULL.
 */
